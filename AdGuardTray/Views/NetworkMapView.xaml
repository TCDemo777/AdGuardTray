<UserControl x:Class="AdGuardTray.Views.NetworkMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.WindowBackground}"
             Foreground="{DynamicResource Brush.TextPrimary}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="16"/><RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="260"/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Style="{DynamicResource Text.PageTitle}" Text="Network Map"/>
                <TextBlock Margin="0,5,0,0" Style="{DynamicResource Text.Secondary}"
                           Text="A live hierarchy built from the existing dashboard client snapshot."/>
            </StackPanel>
            <TextBox Grid.Column="1" Height="38" Padding="10,7"
                     VerticalContentAlignment="Center"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <Border Style="{DynamicResource Card.Compact}" HorizontalAlignment="Stretch">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock FontSize="17" FontWeight="SemiBold" Text="Internet"/>
                            <TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Secondary}" Text="WAN connectivity"/></StackPanel>
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" FontWeight="SemiBold" Text="{Binding InternetStatus}"/>
                    </Grid>
                </Border>
                <TextBlock Margin="20,4" FontSize="20" Foreground="{DynamicResource Brush.TextSecondary}" Text="↓"/>
                <Border Style="{DynamicResource Card.Compact}">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock FontSize="17" FontWeight="SemiBold" Text="{Binding RouterName}"/>
                            <TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Secondary}" Text="Router"/></StackPanel>
                        <StackPanel Grid.Column="1"><TextBlock HorizontalAlignment="Right" FontWeight="SemiBold" Text="{Binding RouterStatus}"/>
                            <TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Muted}" Text="{Binding LastUpdated}"/></StackPanel>
                    </Grid>
                </Border>
                <TextBlock Margin="20,4" FontSize="20" Foreground="{DynamicResource Brush.TextSecondary}" Text="↓"/>
                <ItemsControl ItemsSource="{Binding Networks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Expander Margin="0,0,0,10" IsExpanded="True">
                                <Expander.Header>
                                    <Grid Width="700">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding Name}"/>
                                        <TextBlock Grid.Column="1" Style="{DynamicResource Text.Secondary}" Text="{Binding Status}"/>
                                    </Grid>
                                </Expander.Header>
                                <ItemsControl Margin="24,8,0,0" ItemsSource="{Binding Children}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Margin="0,0,0,8" Padding="14" HorizontalContentAlignment="Stretch"
                                                    Style="{DynamicResource Button.Secondary}"
                                                    Tag="{Binding}" Click="Device_Click">
                                                <Grid>
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="170"/><ColumnDefinition Width="130"/></Grid.ColumnDefinitions>
                                                    <StackPanel><TextBlock FontWeight="SemiBold" Text="{Binding Name}"/>
                                                        <TextBlock Margin="0,3,0,0" Style="{DynamicResource Text.Secondary}">
                                                            <Run Text="{Binding Manufacturer}"/><Run Text=" · "/><Run Text="{Binding DeviceType}"/>
                                                        </TextBlock></StackPanel>
                                                    <StackPanel Grid.Column="1"><TextBlock Text="{Binding IpAddress}"/>
                                                        <TextBlock Margin="0,3,0,0" Style="{DynamicResource Text.Muted}" Text="{Binding TypicalOnlinePeriod}"/></StackPanel>
                                                    <StackPanel Grid.Column="2"><TextBlock HorizontalAlignment="Right" FontWeight="SemiBold" Text="{Binding Status}"/>
                                                        <TextBlock Margin="0,3,0,0" HorizontalAlignment="Right" Style="{DynamicResource Text.Muted}" Text="{Binding LastSeenDisplay}"/></StackPanel>
                                                </Grid>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
