<UserControl x:Class="AdGuardTray.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page heading -->

        <StackPanel Grid.Row="0">

            <TextBlock
                FontSize="24"
                FontWeight="Bold"
                Text="Analytics"/>

            <TextBlock
                Margin="0,5,0,0"
                FontSize="14"
                Foreground="#666666"
                Text="Historical AdGuard Home statistics and DNS activity"/>

        </StackPanel>

        <!-- DNS query history -->

        <Border
            Grid.Row="2"
            Padding="16"
            Background="White"
            BorderBrush="#DDDDDD"
            BorderThickness="1"
            CornerRadius="10">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    FontSize="18"
                    FontWeight="Bold"
                    Text="DNS Query History"/>

                <lvc:CartesianChart
                    Grid.Row="2"
                    MinHeight="300"
                    Series="{Binding QueryHistorySeries}"/>

            </Grid>

        </Border>

    </Grid>

</UserControl>