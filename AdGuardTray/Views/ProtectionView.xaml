<UserControl x:Class="AdGuardTray.Views.ProtectionView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <UserControl.Resources>
  <Style x:Key="Card" TargetType="Border"><Setter Property="Margin" Value="6,6,6,10"/><Setter Property="Padding" Value="18"/><Setter Property="Background" Value="White"/><Setter Property="CornerRadius" Value="10"/><Setter Property="BorderBrush" Value="#DDDDDD"/><Setter Property="BorderThickness" Value="1"/></Style>
  <Style x:Key="ActionButton" TargetType="Button"><Setter Property="Height" Value="36"/><Setter Property="MinWidth" Value="105"/><Setter Property="Margin" Value="0,0,9,9"/><Setter Property="Padding" Value="12,0"/></Style>
 </UserControl.Resources>
 <Grid>
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="12"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Grid Grid.Row="0">
   <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
   <StackPanel><TextBlock FontSize="24" FontWeight="Bold" Text="Protection"/><TextBlock Margin="0,5,0,0" FontSize="14" Foreground="#666" Text="Protection, filtering, profiles, blocked services and DNS rules"/></StackPanel>
   <Button Grid.Column="1" Width="110" Height="36" Content="Refresh all" Command="{Binding RefreshAllCommand}"/>
  </Grid>
  <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
   <StackPanel>
    <Grid>
     <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <Border Grid.Column="0" Style="{StaticResource Card}">
      <StackPanel><TextBlock FontSize="18" FontWeight="Bold" Text="Status"/><Separator Margin="0,8"/><TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding StatusText}"/><TextBlock Margin="0,8,0,0" Foreground="#555" TextWrapping="Wrap" Text="{Binding StatusDetail}"/><TextBlock Margin="0,7,0,0" FontWeight="SemiBold" Text="{Binding Remaining}"/><WrapPanel Margin="0,16,0,0"><Button Style="{StaticResource ActionButton}" Content="Enable" Command="{Binding EnableProtectionCommand}"/><Button Style="{StaticResource ActionButton}" Content="Disable" Command="{Binding DisableProtectionCommand}"/><Button Style="{StaticResource ActionButton}" Content="Resume" Command="{Binding ResumeProtectionCommand}"/></WrapPanel><TextBlock Margin="0,10,0,5" FontWeight="SemiBold" Text="Pause presets"/><WrapPanel><Button Style="{StaticResource ActionButton}" Content="30 minutes" Command="{Binding Pause30Command}"/><Button Style="{StaticResource ActionButton}" Content="1 hour" Command="{Binding Pause1HourCommand}"/><Button Style="{StaticResource ActionButton}" Content="4 hours" Command="{Binding Pause4HoursCommand}"/><Button Style="{StaticResource ActionButton}" Content="Until tomorrow" Command="{Binding PauseUntilTomorrowCommand}"/></WrapPanel></StackPanel>
     </Border>
     <Border Grid.Column="2" Style="{StaticResource Card}">
      <StackPanel><TextBlock FontSize="18" FontWeight="Bold" Text="Protection options"/><Separator Margin="0,8"/><CheckBox Margin="0,7" FontWeight="SemiBold" Content="DNS filtering" IsEnabled="{Binding ControlsEnabled}" IsChecked="{Binding FilteringEnabled, Mode=TwoWay}"/><CheckBox Margin="0,7" FontWeight="SemiBold" Content="Safe Browsing" IsEnabled="{Binding ControlsEnabled}" IsChecked="{Binding SafeBrowsingEnabled, Mode=TwoWay}"/><CheckBox Margin="0,7" FontWeight="SemiBold" Content="Safe Search" IsEnabled="{Binding ControlsEnabled}" IsChecked="{Binding SafeSearchEnabled, Mode=TwoWay}"/><CheckBox Margin="0,7" FontWeight="SemiBold" Content="Parental Control" IsEnabled="{Binding ControlsEnabled}" IsChecked="{Binding ParentalEnabled, Mode=TwoWay}"/><CheckBox Margin="0,7" FontWeight="SemiBold" Content="Query logging" IsEnabled="{Binding ControlsEnabled}" IsChecked="{Binding QueryLogEnabled, Mode=TwoWay}"/><TextBlock Margin="0,15,0,4" FontWeight="SemiBold" Text="Current profile"/><TextBlock FontSize="18" FontWeight="Bold" Text="{Binding ProfileName}"/><WrapPanel Margin="0,13,0,0"><Button Style="{StaticResource ActionButton}" Content="Standard" Command="{Binding ApplyStandardProfileCommand}"/><Button Style="{StaticResource ActionButton}" Content="Family" Command="{Binding ApplyFamilyProfileCommand}"/><Button Style="{StaticResource ActionButton}" Content="Privacy" Command="{Binding ApplyPrivacyProfileCommand}"/></WrapPanel></StackPanel>
     </Border>
    </Grid>

    <Border Style="{StaticResource Card}">
     <StackPanel><DockPanel><TextBlock FontSize="18" FontWeight="Bold" Text="Blocked services"/><Button DockPanel.Dock="Right" Width="110" Height="34" Content="Save changes" Command="{Binding SaveBlockedServicesCommand}"/></DockPanel><Separator Margin="0,8"/><TextBlock Foreground="#555" Text="Select services to block globally, then save the changes."/><ListBox Margin="0,12,0,0" Height="230" ItemsSource="{Binding BlockedServices}" BorderThickness="0"><ListBox.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ListBox.ItemsPanel><ListBox.ItemTemplate><DataTemplate><CheckBox Width="180" Margin="4,5" Content="{Binding Name}" IsChecked="{Binding IsBlocked, Mode=TwoWay}" IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}"/></DataTemplate></ListBox.ItemTemplate></ListBox></StackPanel>
    </Border>

    <Grid>
     <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <Border Grid.Column="0" Style="{StaticResource Card}">
      <StackPanel><TextBlock FontSize="18" FontWeight="Bold" Text="Allow and block rules"/><Separator Margin="0,8"/><TextBlock Foreground="#555" Text="Enter a domain without http:// or a path."/><Grid Margin="0,10,0,10"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Height="34" Padding="8,5" Text="{Binding NewRuleDomain, UpdateSourceTrigger=PropertyChanged}"/><Button Grid.Column="1" Margin="8,0,0,0" Width="85" Content="Block" Command="{Binding AddDenyRuleCommand}"/><Button Grid.Column="2" Margin="8,0,0,0" Width="85" Content="Allow" Command="{Binding AddAllowRuleCommand}"/></Grid><ListBox Height="190" ItemsSource="{Binding FilteringRules}" SelectedItem="{Binding SelectedRule}" DisplayMemberPath="Display"/><Button Margin="0,10,0,0" Width="110" Height="34" HorizontalAlignment="Left" Content="Delete selected" Command="{Binding DeleteRuleCommand}"/></StackPanel>
     </Border>
     <Border Grid.Column="2" Style="{StaticResource Card}">
      <StackPanel><TextBlock FontSize="18" FontWeight="Bold" Text="DNS rewrites"/><Separator Margin="0,8"/><Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="8"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Domain"/><TextBox Height="34" Padding="8,5" Text="{Binding NewRewriteDomain, UpdateSourceTrigger=PropertyChanged}"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Text="Answer (IP or domain)"/><TextBox Height="34" Padding="8,5" Text="{Binding NewRewriteAnswer, UpdateSourceTrigger=PropertyChanged}"/></StackPanel></Grid><Button Width="105" Height="34" HorizontalAlignment="Left" Content="Add rewrite" Command="{Binding AddRewriteCommand}"/><ListBox Margin="0,10,0,0" Height="190" ItemsSource="{Binding DnsRewrites}" SelectedItem="{Binding SelectedRewrite}" DisplayMemberPath="Display"/><Button Margin="0,10,0,0" Width="110" Height="34" HorizontalAlignment="Left" Content="Delete selected" Command="{Binding DeleteRewriteCommand}"/></StackPanel>
     </Border>
    </Grid>
    <Border Margin="6,4,6,8" Padding="14" Background="#F8FAFC" CornerRadius="8" BorderBrush="#D7DDE5" BorderThickness="1"><TextBlock Foreground="#444" TextWrapping="Wrap" Text="{Binding Message}"/></Border>
   </StackPanel>
  </ScrollViewer>
 </Grid>
</UserControl>
